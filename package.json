{
  "name": "robinhood-leaderboard",
  "version": "1.0.0",
  "description": "On-chain Builder Leaderboard for Robinhood Chain Testnet",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network robinhood",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
